spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    baseline-on-migrate: true
    baseline-version: 22

logging:
  level:
    com.neogaming: DEBUG
    org.springframework.security: DEBUG
    org.flywaydb: INFO
