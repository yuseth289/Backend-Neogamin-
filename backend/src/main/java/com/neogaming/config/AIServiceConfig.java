package com.neogaming.config;

import org.springframework.context.annotation.Configuration;

// AIServiceClient uses java.net.http.HttpClient directly — no RestClient bean needed.
@Configuration
public class AIServiceConfig {
}
