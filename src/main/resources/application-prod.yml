spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  mail:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    com.neogaming: INFO
    org.springframework.security: WARN
    org.flywaydb: INFO
